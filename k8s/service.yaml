apiVersion: v1
kind: Service
metadata:
  name: smilecare-dental-service
  namespace: smilecare-dental
  labels:
    app: smilecare-dental
spec:
  type: NodePort
  selector:
    app: smilecare-dental
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
